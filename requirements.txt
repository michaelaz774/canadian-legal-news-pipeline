# Core data collection
feedparser==6.0.10
beautifulsoup4==4.12.2
lxml==5.1.0
requests==2.31.0

# AI/LLM
openai==1.12.0
anthropic>=0.40.0  # Updated for compatibility with modern httpx
google-genai==1.0.0  # New unified Google AI SDK (replaces deprecated google-generativeai)
google-api-core>=2.29.0  # Required for exception handling (ResourceExhausted, ServiceUnavailable)
pydantic==2.5.0  # Schema validation for structured AI outputs
tenacity==8.2.3  # Retry logic with exponential backoff

# Utilities
python-dotenv==1.0.0
python-dateutil==2.8.2
tqdm==4.66.1

# Web Interface
streamlit>=1.29.0
watchdog==3.0.0  # Required for Streamlit file watching

# Optional: Scheduling
schedule==1.2.0

# Production dependencies
gunicorn==21.2.0  # WSGI server (if needed for non-Streamlit deployments)
